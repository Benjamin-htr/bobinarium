{
  "name": "bobinarium",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel --stream --filter \"./*\" --if-present run dev",
    "build": "pnpm -r --parallel --stream --filter \"./*\" --if-present run build",
    "test": "pnpm -r --parallel --stream --filter \"./*\" --if-present run test",
    "typecheck": "pnpm -r --parallel --stream --filter \"./*\" --if-present run typecheck",
    "lint": "pnpm -r --parallel --stream --filter \"./*\" --if-present run lint",
    "format": "pnpm -r --parallel --stream --filter \"./*\" --if-present run format",
    "rm-modules": "find . -name 'node_modules' -type d -exec rm -rf '{}' +"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.11.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "typescript": "catalog:"
  }
}
